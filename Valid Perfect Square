class Solution {
public:
    bool isPerfectSquare(int num) {
        int x=sqrt(num);
        if(num==pow(x,2) ){
            return true;
        }
        else{
            return false;
        }
        
    }
};
